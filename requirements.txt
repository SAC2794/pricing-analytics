# requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Versiones fijadas para garantizar reproducibilidad en Azure Functions.
# ─────────────────────────────────────────────────────────────────────────────

# ── Azure SDKs ────────────────────────────────────────────────────────────────
azure-core==1.38.2
azure-storage-blob==12.28.0
azure-identity>=1.15.0          # Managed Identity (DefaultAzureCredential)
azure-functions>=1.18.0         # Azure Functions HTTP triggers

# ── Base HTTP / networking ────────────────────────────────────────────────────
certifi==2026.2.25
charset-normalizer==3.4.4
idna==3.11
requests==2.32.5
urllib3==2.6.3
isodate==0.7.2

# ── Cryptography / C extensions ──────────────────────────────────────────────
cffi==2.0.0
pycparser==3.0
cryptography==46.0.5

# ── Data ─────────────────────────────────────────────────────────────────────
numpy==2.4.2
pandas==3.0.1
python-dateutil==2.9.0.post0
pytz>=2024.1                    # zona horaria (usado por pandas internamente)
tzdata==2025.3
six==1.17.0
typing_extensions==4.15.0

# ── Database ─────────────────────────────────────────────────────────────────
pyodbc==5.3.0
SQLAlchemy==2.0.47
greenlet==3.3.2                 # requerido por SQLAlchemy async

# ── Machine Learning ─────────────────────────────────────────────────────────
scikit-learn>=1.4.0

# ── Utilidades ───────────────────────────────────────────────────────────────
# python-dotenv se mantiene por compatibilidad con entornos locales,
# aunque el pipeline no lo usa (corre 100% con Managed Identity).
python-dotenv==1.2.1